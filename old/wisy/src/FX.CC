#include <cregion.h>
#include <grx.h>
#include <stdio.h>
#include <general.h>

void movingrect(cregion *newreg, cregion *oldreg)
{
  GrSetContext(NULL);

  if (oldreg)
        {
        }
}
